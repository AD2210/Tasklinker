{% extends 'base.html.twig' %}

{% set action = app.current_route == 'app_task_new' ? 'Nouvelle tâche' : (task.title) %}
{% block title %}{{ action }} - {{ parent()}}{% endblock %}

{% block body %}
    <main>
        <header>
            <div id="current-user"></div>
            <div id="title-page">
                <h1>{{ action }}</h1>
            </div>
        </header>
        <div id="content">
        </div>
    </main>
{% endblock %}